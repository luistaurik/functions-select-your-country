// this are the exact requests

// 1.0

function firstVersion(a, b, c){
    return a+b+c
    }

// 1.1

var secondVersion = function(a, b, c){
    return a+b+c;
}
console.log(secondVersion(3,7,5));

// 2.0

expresedFunction = function(a, b){
    return a + b
    }

// 2.1

arrowFunction = (a,b) => a + b;
console.log(arrowFunction(3,7));
